<html lang="en">
<head>
    <title>Exercise 4</title>
    <script>
        function f4(a, b, l) {
            let sum = 0;
            for (let num of l) {
                if (num % a === 0 || num % b === 0) {
                    sum += num;
                }
            }
            return sum;
        }

        function doIt() {
            let a = parseInt(document.getElementById("a").value);
            let b = parseInt(document.getElementById("b").value);
            let l = document.getElementById("l").value.split(",").map(num => parseInt(num.trim()));
            if (isNaN(a) || isNaN(b) || l.some(isNaN) || a <= 0 || b <= 0) {
                alert("Please enter number for a, b, and a list of integers for l.");
                return;
            }
            let result = f4(a, b, l);
            alert(`The sum of all multiples of ${a} or ${b} in the list is: ${result}`);
        }
    </script>
</head>
<body>
    <h2>Find the Sum of Multiples in a List</h2>
    <p>Enter values for a, b, and a list of numbers then press the button.</p>

    <label for="a">a:</label>
    <input type="number" id="a" placeholder="Enter a"><br><br>

    <label for="b">b:</label>
    <input type="number" id="b" placeholder="Enter b"><br><br>

    <label for="l">List:</label>
    <input type="text" id="l"><br><br>

    <button onclick="doIt()">Press me!</button>
</body>
</html>
